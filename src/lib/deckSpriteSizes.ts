/** Tamaños (px) de `DeckSpriteImg` en cada vista — iconos de menú; un poco más grandes en UI “principal”. */

/** Tabla pública Torneos (# / jugador / mazo) — tamaño por defecto de `DeckSpriteImg` si no se pasa `size`. */
export const DECK_SPRITE_TORNEOS_PX = 88;

/** Torneos: base para maxDim; con `plainFit="square-cover"` el cuadrado recorta transparente (~75px). */
export const DECK_SPRITE_TORNEOS_TABLE_PX = 54;
export const DECK_SPRITE_TORNEOS_TABLE_INNER_SCALE = 1.38;

/** Escala del dibujo dentro del recuadro en tablas (admin sprites / modal tabla). */
export const DECK_SPRITE_TABLE_INNER_SCALE = 1.52;

/** Pokémon ya elegido en el modal (cabecera de fila). */
export const DECK_SPRITE_MODAL_SELECTED_PX = 120;

/** Opciones del desplegable al buscar. */
export const DECK_SPRITE_MODAL_LIST_PX = 72;

/** Miniaturas en admin · torneos-sprites (tabla) — recuadro; escala interna con `spriteScale`. */
export const DECK_SPRITE_ADMIN_TABLE_PX = 96;
