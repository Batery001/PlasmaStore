# Las variables viven en la raíz del proyecto (junto a package.json de Next).
# Copia la plantilla principal:
#   ../.env.example  →  ../.env.local
#
# Puedes seguir usando este archivo (server/.env) como respaldo; se carga
# después de ../.env y antes de ../.env.local (la raíz gana en local).
#
# Ver pasos numerados en ../.env.example

PORT=3847
